name: First Workflow

on: workflow_dispatch

jobs:
  first-run:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World!!!"
      - name: Print Good Bye
        run: echo "Done -bye!!!"

  second-run:
    runs-on: ubuntu-latest
    steps:
      - name: Print Name
        run: echo "Hello Vishal Agrawal"
